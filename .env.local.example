# 🔐 SECURE ENVIRONMENT VARIABLES
# Copy this file to .env.local and fill in your actual values
# .env.local is automatically ignored by git for security

# =============================================
# CLIENT-SIDE VARIABLES (VITE_ prefix)
# These are safe to expose in the browser
# =============================================

# Supabase configuration (PUBLIC - safe for client-side)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Environment
NODE_ENV=development

# Toggle use of local mock data instead of Supabase
VITE_USE_MOCK_DATA=false

# =============================================
# SERVER-SIDE VARIABLES (NO VITE_ prefix)
# These are NEVER exposed to the browser
# =============================================

# Supabase Service Role Key (SERVER-SIDE ONLY - for database setup)
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_here

# Twilio Configuration (SERVER-SIDE ONLY)
TWILIO_ACCOUNT_SID=your_twilio_account_sid_here
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
TWILIO_PHONE_NUMBER=your_twilio_phone_number_here
TWILIO_VERIFY_SERVICE_SID=your_twilio_verify_service_sid_here

# JWT Secret for session management (SERVER-SIDE ONLY)
JWT_SECRET=your_super_secure_jwt_secret_here_minimum_32_characters

# CSRF Secret (SERVER-SIDE ONLY)
CSRF_SECRET=your_csrf_secret_here_minimum_32_characters

# Allowed origins for CORS (comma-separated)
CORS_ORIGIN=http://localhost:5173,http://localhost:5175

# =============================================
# SECURITY NOTES
# =============================================
# 1. NEVER commit .env.local to version control
# 2. Rotate all keys if they are ever exposed
# 3. Use different keys for development/staging/production
# 4. Keep server-side secrets on the server only
# 5. Use strong, unique secrets for JWT and CSRF
